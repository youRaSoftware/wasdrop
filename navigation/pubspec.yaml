name: navigation
version: 0.0.1
publish_to: none
description: navigation module

environment:
  sdk: ">=3.5.0"
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  features:
    path: ../features

  get_it: ^8.0.3
  go_router: ^14.6.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
