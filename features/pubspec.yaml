name: features
version: 0.0.1
publish_to: none
description: features module

environment:
  sdk: ">=3.5.0"
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter

  core:
    path: ../core
  core_ui:
    path: ../core_ui
  domain:
    path: ../domain

  flame: ^1.38.0
  flame_forge2d: ^0.20.0
  package_info_plus: ^10.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  assets:
    - assets/images/fruits/
    - assets/images/fx/
