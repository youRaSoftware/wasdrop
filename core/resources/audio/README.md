# Audio

Synthesized by `script/gen_placeholder_audio.py` — our own, no licence needed; kept as the shipping sound (decision 2026-09-17). If ever replaced, keep the file names (see `core/lib/services/audio_service.dart`).

- `sfx_tap.wav` — button press
- `sfx_drop.wav` — ball dropped
- `sfx_merge_01..11.wav` — merge, pitch rises with the resulting tier
- `sfx_game_over.wav`, `sfx_record.wav`
- `sfx_shake.wav` — shake bonus (rattle), `sfx_bomb.wav` — bomb bonus (soft boom)
- `music_loop.m4a` — ~21 s seamless loop (AAC; iOS has no OGG)
