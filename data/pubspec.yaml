name: data
version: 0.0.1
publish_to: none
description: data module

environment:
  sdk: ">=3.5.0"
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  domain:
    path: ../domain

  get_it: ^8.0.3
  hive: ^2.2.3
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
